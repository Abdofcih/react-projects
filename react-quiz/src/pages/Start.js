import React from "react";

import { useGlobalContext } from "../context";

import { SetupForm } from "../components";

const Start = () => {
  return <SetupForm />; // if true then you are waiting user info
};

export default Start;
